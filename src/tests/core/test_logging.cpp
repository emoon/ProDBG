#include "test_harness.h"
#include "core/logging.h"

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

TEST(Logging, debug) {
    log_debug("debug %s\n", "debug");
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

TEST(Logging, trace) {
    log_trace("trace %s\n", "trace");
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

TEST(Logging, info) {
    log_info("info %s\n", "info");
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

TEST(Logging, warn) {
    log_warn("warn %s\n", "warn");
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

TEST(Logging, error) {
    log_error("error %s\n", "error");
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

TEST(Logging, fatal) {
    log_fatal("fatal %s\n", "fatal");
}

