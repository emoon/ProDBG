#pragma once

#include <QtCore/QHash>

class QDockWidget;

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class ViewHandler;

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class SourceViewsHandler {
public:
    SourceViewsHandler(ViewHandler* view_handler);
    void open_file(const QString& filename, bool set_active);

private:
    ViewHandler* m_view_handler = nullptr;
};

