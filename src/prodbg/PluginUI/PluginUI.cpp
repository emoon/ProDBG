#include "PluginUI.h"
#include <QWidget>

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

struct PrivData {
	QWidget* parent;
};

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static PUHandle button(void* priv, const char* name, int name_len, void (*callback)(void* user_data)) {
	PrivData* p = (PrivData*)priv;

	return 0;
}
