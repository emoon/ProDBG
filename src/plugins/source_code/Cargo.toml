[package]
name = "source_code"
version = "0.1.0"
authors = ["Daniel Collin <daniel@collin.com>"]

# Type
[lib]
name = "source_code"
crate-type = ["cdylib"]

# Depends
[dependencies]
prodbg_api = { path = "../../../api/rust/prodbg" }

