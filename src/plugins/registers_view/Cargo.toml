[package]
name = "registers_view"
version = "0.0.1"
authors = ["Slava Baginov <slnpacifist@gmail.com>"]

[lib]
name = "registers_view"
crate-type = ["dylib"]

[dependencies]
prodbg_api = { path = "../../../api/rust/prodbg" }
